# GitHub App credentials. Copy to .env locally. Never commit real values.
# On Render, set these as service environment variables (sync: false in render.yaml).

APP_ID=
PRIVATE_KEY=
WEBHOOK_SECRET=

# Optional. Render injects PORT. WEBHOOK_PATH defaults to "/".
# PORT=3000
# HOST=0.0.0.0
# WEBHOOK_PATH=/
