# pr-size-labels
PR Size Labels: a GitHub App bot that labels pull requests size/XS through size/XL from GitHub's own additions+deletions counts in the webhook payload. Replaces the prior size label as PRs grow. No repo cloning; no customer source stored or logged.
